'use client';

export function StepGenerate() {
  return (
    <section className="mx-auto max-w-[1400px] px-[var(--space-8)] py-[var(--space-12)]">
      <h1 className="font-[family-name:var(--font-display)] text-[var(--text-3xl)] tracking-tight">
        Generate
      </h1>
      <p className="mt-[var(--space-4)] text-[var(--color-ink-muted)]">
        Step component stub — filled in by Task 8.
      </p>
    </section>
  );
}
