# M4 Validation Gate — Wizard End-to-End

**Date:** 2026-05-12
**Plan task:** Task 15 — Wizard end-to-end exercise (`docs/superpowers/plans/2026-05-10-frame-bucket-m4.md`)
**Verdict:** _TBD — fill in after the walk-through_

## Setup

| Step               | Artifact                        | Recipe | Cost  |
| ------------------ | ------------------------------- | ------ | ----- |
| Initial generation | `TBD`                           | TBD    | TBD   |
| Round 1            | `TBD`                           | iter 1 | TBD   |
| Round 2            | `TBD`                           | iter 2 | TBD   |
| Round 3 — cap test | (refine panel locks at round 3) | n/a    | $0.00 |
| **Total**          |                                 |        | TBD   |

Brief used: _e.g. an architecture firm or a podcast network — something not previously validated_

---

## Procedure

Walk through the full wizard in order. Note observations under **Findings** below.

- [ ] `/wizard/brief` — enter a NEW brief (not Maple St Bakery, not previously validated)
- [ ] Submit → land on `/wizard/recommend` with exactly one `POST /api/recommend` in Network panel (StrictMode in `next dev`)
- [ ] Pick an aesthetic + layout → click Generate → land on `/wizard/generate`
- [ ] Watch initial generation stream. Confirm exactly one `POST /api/generate`
- [ ] On `done`: round 0 lands in the history sidebar; preview iframe switches to `/preview/<id>`
- [ ] Run round 1 with **structural** feedback (move section, change hero, etc.)
- [ ] Confirm exactly one `POST /api/iterate`; inspect body — **must not contain `previousHtml`** (Rule 1)
- [ ] Run round 2 with any feedback
- [ ] Run round 3 — confirm round 4 hits cap message
- [ ] Toggle "Compare" between two rounds → side-by-side renders both `/preview/<id>` iframes
- [ ] Name a checkpoint on any round → sidebar label updates immediately; Esc/backdrop closes without saving
- [ ] **Refresh the browser.** Confirm wizard state restored: brief, recommendation, rounds, checkpoint name, active round
- [ ] Click "Start over" → state clears; lands on `/wizard/brief` with empty form

---

## Acceptance

- [ ] Wizard never enters an inconsistent state across the procedure
- [ ] Step transitions feel < 200ms (excluding API calls)
- [ ] No console errors at any point
- [ ] Visual hierarchy is intentional — does not look like a default template
- [ ] **Rule 1 holds end-to-end**: any `/api/iterate` request body is < 2 KB and has no `previousHtml`
- [ ] **Rule 2 holds end-to-end**: under StrictMode dev mode, Network panel shows exactly one request per user action — no AbortError-and-retry pairs except on real navigation
- [ ] Subjective: would you hand this to a designer in your network without instructions?

---

## Findings

### Finding 1 — _title_

**Severity:** _Critical | High | Medium | Low_
**Resolution:** _fixed in commit X | open | wontfix_

_What went wrong, what the root cause was, and how it was addressed._

---

### Finding 2 — _title_

_Repeat as needed._

---

## What Worked

_A short list of things that went smoothly. Useful for future-you to compare against if M5+ regresses._

---

## Subjective UX Notes

_Anything that bugged you but isn't a defect. Things to file for M7 polish._

---

## Out of Scope (deferred from this gate)

- Persisted state across browser sessions for the comparison view (intentionally not persisted)
- Mobile responsiveness (M7)
- Public artifact URLs / share links (M5)
- Per-section iteration / cross-recipe iteration (M5+)
